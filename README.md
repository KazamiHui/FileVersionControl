# FileVersionControl
个人实验性项目，版本变动的时间换取更小储存空间消耗.
## 计划
提供以下方法：
`create`:创建一个储存空间。
`read`:读取储存空间中一个文件的一个版本的数据。
`write`:写入储存空间中的一个文件并产生一个新版本。
`delete`:删除储存空间中的一个文件。
`back`:储存空间中的一个文件回退到指定版本并删除该版本后的所有版本。
`get`:获取储存空间中一个文件的多个版本的数据。
